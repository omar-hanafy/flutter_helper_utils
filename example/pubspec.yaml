name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  enefty_icons: ^1.0.7
  dart_helper_utils: ^2.3.0
  intl: ^0.19.0

  # internal packages
  flutter_helper_utils:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
